# Saudi RE Market Intelligence — Environment Variables
# Copy to .env and fill in values

# Path to DuckDB database file
DUCKDB_PATH=data/saudi_re.duckdb

# Path to raw data (sparse-cloned repo)
RAW_DATA_PATH=data/raw/saudi-re-data

# Path to data lake (clean Parquet files)
LAKE_PATH=data/lake
